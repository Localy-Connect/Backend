version: '3'
services:
  localy:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=localy
      - POSTGRES_USER=localy
      - POSTGRES_PASSWORD=localydock
